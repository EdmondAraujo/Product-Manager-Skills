# Session Handoff Brief — August 10, 2026: Lifecycle & EOL Suite (v0.84)

**Supersedes:** `09AUG26.md` + `docs/maintenance/2026-08-09-eol-suite-plan.md` on four points —
see "Where we deviated from the plan" below. Read those two for original intent; read this for what
actually shipped.

---

## What shipped

**v0.84 — 7 new skills + 1 upgrade. Library 70 → 77** (28 Component, 29 Interactive, 20 Workflow).

### `eol-transition` theme (6 skills)

| Skill | Type | Files |
|---|---|---|
| `eol-readiness-advisor` | Interactive | SKILL.md |
| `eol-stakeholder-sequence` | Component | SKILL + template + 2 examples |
| `eol-checklist` | Component | SKILL + template + 2 examples |
| `eol-internal-enablement` | Component | SKILL + template + 2 examples |
| `eol-message` | Component (**upgraded**) | SKILL + template + 2 examples |
| `eol-process` | Workflow | SKILL + template + 2 examples |

### `product-lifecycle` theme (2 skills, added after the plan)

| Skill | Type | Files |
|---|---|---|
| `lifecycle-play-advisor` | Interactive | SKILL.md |
| `product-lifecycle-plays` | Component | SKILL + template + 2 examples |

### Supporting changes

- New pack `07-lifecycle-eol-pack.zip` — added in **two** places (`build-claude-desktop-packs.sh`
  pack list, `build-dist.sh` copy loop **and** its README table)
- README: badges, ASCII banner, prose counts, nav block, pack table, docs table, What's New
- `docs/announcements/2026-08-10-v0-84-lifecycle-and-eol-suite.md`
- `docs/Lifecycle and EOL Suite Summary.md`
- `catalog/` regenerated (77), `dist/` regenerated (77 ZIPs, 9 packages)
- `marketplace.json` → 77 entries + metadata bumped to 0.84.0; `plugin.json` → 0.84

---

## Where we deviated from the plan (and why)

The 09AUG26 plan was approved and then modified in-session by Dean's direction. **Four changes:**

### 1. The tier is a dial the user sets — not passed from upstream

The plan said each skill determines its tier "(a) passed from upstream, or (b) self-classified."
Dean rejected the coupling. **Every skill now recommends a level, presents all three, and honors an
override in either direction**, including mid-run. When a user goes lighter than recommended, the
skill names the specific thing that goes uncovered so it's a choice, not a surprise.

Every skill says Level 2 is the default and **"never default to Level 3."** The rationale is
pedagogic: process theater on a small sunset teaches teams to ignore process on the big one.

### 2. No tight coupling between skills

Dean: *"let's not build this so there are tightly coupled dependencies between the skills, that
breaks the mandate of this repo in general."*

So: each skill defines the levels and gates **inline** (duplicated text, not a shared dependency),
and every References section opens by stating neither direction is a prerequisite. `eol-process` is
a **route, not a pipeline** — it includes an "Entering Mid-Stream" diagnostic mapping symptoms to
the skipped phase, and explicitly says going backwards is a success condition.

**Do not "fix" this by introducing a handoff schema.** Carrying context means saying "Level 2" and
pasting what you have. That is the design.

### 3. "Tier" → "Level"

Shipped skills say **Level 1/2/3 (Light/Standard/Heavy)** throughout. The plan says "Tier." Same
concept, one vocabulary. Keep it.

### 4. Gate vocabulary reconciled: 8 gates, 6 working phases

The source prompts use 6 phases; Phase 1 shipped an 8-gate vocabulary. Reconciled explicitly in
`eol-checklist`:

- **GA is a state, not a phase of work** — nothing gets checklisted there
- **EOR is contract-driven** — insert only when renewals run past EOS
- Working checklists span **NSC, EOS, EOE, EOM, EOL, EOSRV**

The industrial example exercises EOR, so the rule is demonstrated rather than asserted.

### Plus: the suite grew by two skills

The plan was 6 skills. After Phases 1-3 shipped, Dean greenlit the **extend-vs-replace-vs-retire**
fork that had been flagged since the initial gap analysis, as a Component + Interactive pairing
under a new `product-lifecycle` theme. That's `product-lifecycle-plays` + `lifecycle-play-advisor`,
plus two small edits to `eol-readiness-advisor` (upstream pointer at Q1; **Extend** added as a fifth
verdict, explicitly distinguished from Hold).

---

## Dated corrections to the plan doc

`09AUG26.md` pencils the NFA-200 industrial universe as *"EOL announcement Q4 2026 with EOSRV in
Q2 2027."* **That is inconsistent** with the universe as built — service contracts run through 2028,
so EOSRV cannot precede them. Shipped dates:

- NSC Mar 31 2026 · EOS Mar 31 2027 · EOE Mar 31 2027 · EOR Dec 31 2027 · EOM Dec 31 2028 ·
  **EOL not scheduled** · EOSRV Dec 31 2028

SaaS universe (Fieldlight Classic Dispatch), unchanged and consistent across 10 files:
NSC Jan 15 2026 · EOS Mar 1 · EOE Jun 1 · EOM Oct 1 · EOL Dec 31 2026 · 800 accounts · $2.4M ARR ·
~30 custom-rule accounts · 94% retention against a 90% target.

---

## What we learned this session

### The universes should interlock across skills, not just within them

The strongest pedagogy came from letting one story run through every skill and **letting it fail**.
The Northfield/NFA-200 thread: a play decision that was correct, a risk register that named the
right hazard and rated it "Low" on an assumption, a Phase 2 that broke the plan, a return to
Phase 1, and an outcome of End of Sale with no EOL date. The `product-lifecycle-plays` industrial
example annotates *where the miss was visible at the time.*

A clean worked example teaches the format. A failure worked forward teaches the judgment.

### "Not scheduled" is a first-class output

Repeatedly, the honest artifact was an absence: an EOL date deliberately unset with a recorded
precondition and a named owner to re-check it. Templates now support this explicitly, because the
alternative — an invented date to fill a blank — is a promise you break in public.

### The gate that matters is the one after stakeholder alignment

DP2 in `eol-process`. Both the SaaS and industrial examples turn on it (one clears it explicitly,
one fails it and loops). Teams treat it as a formality; it's the cheapest place in the whole
process to discover you're wrong.

### Validator gotcha: H2 phase headers make `## Application` read as empty

`eol-process` initially failed smoke with *"section 'Application' is empty"* because `## Phase 1:`
followed `## Application` immediately. `discovery-process` solves this with a lead-in paragraph. Do
the same in any future Workflow skill.

---

## Repo gotchas confirmed or added

- **README banner is 70 *characters* per line** (not bytes — the `║` and `•` are multibyte).
  Verify with `len(line)` in Python after any edit. Changing "July 17, 2026" → "August 10, 2026"
  required removing exactly 2 spaces of padding.
- **`build-dist.sh` hardcodes the pack list in two places** — the copy loop (~line 39) and the
  generated README table (~line 125). Confirmed again this session.
- **`marketplace.json` is hand-maintained**; `check-library-drift.py` catches missing entries.
- **`check-library-drift.py` does NOT check `catalog/`, `dist/`, or prose counts.** CI can be fully
  green while all three are stale. Regenerate with `generate-catalog.py` and `build-dist.sh`, and
  update README counts by hand.
- Packs are **ZIPs of ZIPs** (one inner ZIP per skill) — that's the existing convention, not a bug.
- The one persistent trigger warning is `incoming-request-advisor` (description lacks a literal
  "Use when"). Pre-existing, unrelated, still there.

---

## Verification run before commit

```
scripts/validate-skills.sh   -> 77 skills, 0 failures, no drift
scripts/test-a-skill.sh --smoke skills/eol-*/SKILL.md
                             -> 6/6 PASS, 0 warn
scripts/check-skill-triggers.py -> 0 errors, 1 warning (pre-existing)
scripts/generate-catalog.py  -> 77 skills, 6 commands
scripts/build-dist.sh        -> 77 skill ZIPs, 9 packages
```

---

## Part 2 — after the suite shipped

The EOL suite was released as v0.84, then the session continued through the open queue. Everything
below also shipped Aug 10.

### Library-wide metadata backfill

All **77/77** skills now carry `theme`, `best_for`, `scenarios`, and `estimated_time`. Coverage was
42/77 on theme and 43/77 on estimated_time; 35 skills fell out of themed navigation into the "All
other skills" bucket.

- New theme **`validation-experiments`** (pol-probe, pol-probe-advisor, derisk-measurement-advisor,
  lean-ux-canvas, recommendation-canvas) — the group had no home and didn't fit discovery or artifacts
- `company-intel` and `tam-sam-som-calculator` were part of the v0.83 market-intel suite and had
  never been tagged; `pestel-analysis` now sits with its sibling `pestel-delta-monitor`
- `best_for` and `scenarios` written for the 15 skills that had neither

### The CI hole, closed

`check-library-drift.py` verifies marketplace and doc links but **not** `catalog/`, `dist/`, or prose
counts — so CI can be fully green while the browsable shelf advertises the previous release. That is
exactly what happened between v0.83 and v0.84.

**`scripts/check-dist-freshness.py`** now verifies catalog membership and count, one `dist/` ZIP per
skill with no orphans, every declared pack in `dist/packages/`, and that `dist/CATALOG.md` mentions
everything.

Two design constraints, both load-bearing:
1. **Content-level, never byte-level.** Rebuilt ZIPs differ every run from embedded timestamps, so
   comparing archive bytes would fail constantly and train everyone to ignore the check.
2. **Its own CI step, NOT inside `validate-skills.sh`.** `build-dist.sh` → `build-release.sh` →
   `validate-skills.sh`, so a check living there would abort the very command that fixes the
   staleness it detects. Also wired in as step 7 of `test-library.sh`, after catalog regeneration.

Verified by adding a throwaway skill: four specific findings plus a fix command, clean again on removal.

### Provenance rule — and two anonymization traps

Dean flagged that citing named deliverables ("Dean's GTM/EOL Strategy Workshop") is identifiable even
with a client scrubbed. **The rule, now in CONTRIBUTING.md and CLAUDE.md:** cite public, linkable
work by name; for unpublished internal or client-delivered material, cite the framework or the
practice, not the artifact.

Rewritten across 13 spots spanning v0.83 and v0.84, plus `organic-growth-advisor`, CLAUDE.md, and the
09AUG/10AUG/17JUL briefs — including local file paths that carried a client name.

**Two traps worth carrying forward, both of which bit this repo today:**
1. A named deliverable + a date + an author is findable even with the client scrubbed
2. **A committed denylist is itself a disclosure.** An earlier version of the Plan 4 guardrail
   enumerated the topics to avoid, which points a reader at the sensitive material. Exclusions live
   in session memory; committed docs state the constraint positively (which domain to stay *inside*)

Kept unchanged because public and linkable: product-manager-prompts URLs, Product Porch episode URLs,
third-party frameworks, productside.com as a company link.

### Published assets rebuilt

The `v0.84` release assets carried the old provenance (the tag predated the scrub). Verified by
downloading a live ZIP rather than assuming. **Tag moved to `7f92c40`**, workflow rebuilt every
asset, re-verified clean. Commit history deliberately left alone — a rewrite costs more, reaches
neither forks nor clones, and what was exposed is a product category and training-deliverable names,
never a client.

### Adornment Plan 4 — COMPLETE

All four waves. Plan and full detail: `docs/maintenance/2026-08-10-adornment-plan-4.md`.

| Wave | Scope |
|---|---|
| 1 | 4 template-only fixes |
| 2 | 7 core artifacts (Northfield) |
| 3 | 4 workflows — **Brightwater Biologics debuts** |
| 4 | 10 single-domain + 3 Group A skills |

**Single-domain gap 21 → 0**, excluding three documented carve-outs. Every Component and Workflow
skill now ships `template.md` plus two-domain worked examples.

**Third universe approved and in use:** Brightwater Biologics — life sciences, **clinical trial
operations**, platform **Trialpath**. Scoped deliberately, not a general fallback: used only where
regulated / evidence-driven / long-cycle work IS the lesson. Core artifacts keep an instantly legible
second domain, because a domain needing vocabulary setup competes with the lesson in the library's
highest-traffic skills. See `memory/fictional-universes.md` for the guardrail.

### More learning

- **A failure worked forward teaches more than a clean example.** The strongest artifacts this
  session were the ones where the worked example *fails* — a risk register that rated the right
  hazard "Low" on an assumption, an epic correctly killed after three weeks, a Phase 1 verdict marked
  Superseded rather than deleted. Reuse this pattern.
- **Both stakeholder-mapping examples independently landed the most-affected, least-powerful group
  in *Monitor* on the first grid** and corrected it on the second. Same pattern, unrelated domains —
  the mismatch looks structural, and both files say so.
- **Trigger audit is clean library-wide for the first time.** `incoming-request-advisor` said "Use
  before replying" where `check-skill-triggers.py` requires a literal "Use when."

---

## What's next

*(Rewritten at end of session. The four near-term items previously listed here — theme backfill,
Adornment Plan 4, the dist/ CI gap, and the trigger warning — all shipped today.)*

### 1. Triage the community queue — do this first

**Three open PRs and five open issues arrived while the library was being built.** Nothing here has
been reviewed, and it's the only category with someone waiting on the other end.

| PR | Size | Notes |
|---|---|---|
| **#24** docs: one-click trial for the PRD workflow (@arimu1) | 1 file, +14/-2 | Trivial. Read it and merge or close today |
| **#23** fix(plugin): single marketplace plugin + move commands README (@arimu1) | 4 files, **+49/-944** | **Needs real scrutiny.** The deletion count suggests it collapses the 77 per-skill `marketplace.json` entries into one plugin. That is an architecture change to how Claude Code users discover skills, and `check-library-drift.py` currently asserts the per-skill model. Decide the model *before* reviewing the diff |
| **#20** Add OpenCode runtime compatibility (@dNzZiz) | 60 files, +989/-620 | Long-standing. Per session memory the ask was a docs-only slim-down, requested ~end of July, otherwise build it ourselves with credit. Time to resolve either way |

**Issues:**
- **#14 `organic-growth-advisor` missing from marketplace.json — ALREADY FIXED.** Verified present;
  `check-library-drift.py` would fail otherwise. **Close it.**
- **#19** and **#18** appear to duplicate PRs #20 and #24 — close alongside whatever happens to those
- **#25** Chinese PM Skill Navigator proposal — a scope/i18n decision, not a code review. Worth a
  considered answer
- **#22** "87 site" — unclear, needs a read

### 2. Ship a v0.85 when ready

The `v0.84` tag sits at `7f92c40`; `main` has moved well past it. Unreleased and on main: the
77/77 metadata backfill, `check-dist-freshness.py`, the provenance corrections, and all four
adornment waves. None of it is urgent — but the published assets are a day behind the library, and
the next release should carry it.

### 3. Small, high-value items

- **Two interactive skills carry thin examples** — `intel-discipline-advisor` (one conversation
  flow) and `workshop-facilitation` (a protocol skill, arguably exempt by design — decide rather
  than default). The other 27 of 29 are fine, so this is a spot fix, not a Plan 5
- **"Invoking skills with arguments"** subsection in `docs/Using PM Skills with Claude.md`
- **Decide the fate of the v0.80 AI Product Builder Track brief** (`15MAY26.md`) — planned for v0.80,
  never executed, still sitting there

### 4. New skill work, in rough priority order

**Reframed from the EOL follow-ons.** Dean's read: portfolio screening and decision rights
*transcend past EOL* — they're general PM skills that happen to have EOL applications, and should be
built as general skills the EOL suite cross-references rather than as suite members.

- **Migration & retention campaign** — the one genuinely EOL-shaped gap left. "Lose the product
  without losing the customer" is the suite's thesis; `eol-internal-enablement` arms the *teams* and
  `eol-message` writes the *announcement*, but the customer-facing retention campaign itself is
  still implicit
- **Portfolio screening** (general) — candidate criteria and a screening pass across a catalog.
  Partially covered by `product-lifecycle-plays`' portfolio worksheet
- **Decision rights / DACI** (general) — who drives, approves, contributes, is informed. The
  stakeholder skills cover conversation order and engagement, not authority
- **Contract / SLA / regulatory obligation review** — currently one stop in the EOL sequence and one
  checklist area, but it is the failure mode behind every named EOL disaster
- **Phase 6 AI PM Orchestrator** — `ai-product-evals`, `ai-observability-framework`,
  `ai-maintenance-planning`, `ai-product-orchestrator`
- **Pricing & Monetization Suite** (7 skills)
- **VP/CPO → C-suite transition** — no source material yet

### 5. Standing conventions — do not regress these

1. **Right-sizing is a dial the user sets**, never auto-assigned. Recommend, honor the override,
   name what a lighter level leaves uncovered
2. **No tight coupling between skills.** Orchestrators are recommended routes through independent
   stops. No handoff schemas
3. **Provenance:** cite public linkable work by name; for unpublished internal or client-delivered
   material cite the framework, not the artifact. **Never commit a denylist**
4. **Brightwater is scoped**, not a general third domain — see `memory/fictional-universes.md`
5. **`check-dist-freshness.py` stays outside `validate-skills.sh`** — moving it in creates a
   self-abort loop

---

## Source material

| Source | Location |
|---|---|
| 5 EOL prompts | `~/Code/product-manager-prompts/prompts/eol-*.md` |
| Blog post | `~/Downloads/eol/eol-post.md` |
| DOCX checklists (2) | `~/Downloads/eol/*.docx` |
| Webinar deck | _(local, not in repo)_ |
| Workshop deck | _(local, not in repo)_ |

**Anonymization held:** no client names and no details carried over from the source client
artifacts appear anywhere in the shipped skills. Fictional universes only. The real-company failure
cases used in `product-lifecycle-plays` (Kodak, Zune, New Coke, Vista, Nest Revolv, AT&T POTS,
Amgen) are public-record business history from Dean's own deck, not customer engagements.
